namespace Sorolla
{
    /// <summary>
    /// Notification types for haptic feedback.
    /// </summary>
    public enum HapticsType
    {
        /// <summary>UI selection change feedback.</summary>
        Selection,
        /// <summary>Positive notification (success, completion).</summary>
        Success,
        /// <summary>Caution notification (warning).</summary>
        Warning,
        /// <summary>Negative notification (error, failure).</summary>
        Error
    }
}
